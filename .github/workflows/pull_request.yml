name: Pull Request Node CI
on: [pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.4

      - name: Setup Node.js 16 🛠️
        uses: actions/setup-node@v2
        with:
          node-version: 16

      - name: Install 🔧
        run: yarn --no-lockfile --non-interactive

      - name: Build 🔨
        run: yarn build
